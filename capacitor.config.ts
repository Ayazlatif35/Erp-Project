import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shiftly.workforceconnect',
  appName: 'com.shiftly.workforceconnect',
  webDir: 'dist'
};

export default config;
